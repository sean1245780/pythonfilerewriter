Welcome to the file PythonHtmlCon!
By importing it and using it with the right parameters, you'll get the right, wanted, result.

The example file is "example.py"
It uses the "somehtml.html" for the example.
To see the outcome, write in the url of your browser:  localhost:8910
It creates a server whenever you activate the "example.py" file.
Have fun!

* Warning: Read the instruction inside the PythonHtmlCon and remember that the program can read only
encoded text and not other byte made files like images!